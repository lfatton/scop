#ifndef FACE_HPP
# define FACE_HPP

# include <vector>

class Face {
private:
public:
    int a, b, c, d;

    explicit Face(unsigned int a = 0, unsigned int b = 0, unsigned int c = 0, unsigned int d = 0);
};

#endif //FACE_HPP
